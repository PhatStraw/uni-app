import {roboto} from '@theme-ui/presets'
import {merge} from 'theme-ui'

// example theme.js
export default merge(roboto,{
    colors: {
      primary: 'rgb(35,72,163)',
      secondary: 'rgb(16,21,46)',
      border: 'lightgrey'
    }
  })

  